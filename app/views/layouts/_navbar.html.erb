<nav class="navbar navbar-dark">
  <div class='container'>
    <div class='navbar-header'>
<%= link_to 'F', all_items_path, class:"navbar-brand" %>
</div>

  <ul class="nav navbar-nav">
    <li class='dropdown'>
      <a href ="#" class="dropdown-toggle" role="button" data-toggle="dropdown">Items by Category<span class='caret'></span>
      </a>
      <ul class='dropdown-menu'>
        <% Category.all.each do |cat| %>
        <li>
          <%= link_to cat.name, categorical_path(id: cat) %>
        </li>
        <% end %>
      </ul>
    </li>

    <li class='dropdown'>
      <a href ="#" class="dropdown-toggle" role="button" data-toggle="dropdown">Items by Brand
        <span class='caret'></span>
      </a>
      <ul class='dropdown-menu'>
        <% @brands.each do |brand| %>
        <li>
          <%= link_to brand, branding_path(brand_name: brand) %>
        </li>
        <% end %>
      </ul>
    </li>

  </ul>

  <ul class='nav navbar-nav navbar-right'>
    <li>
      <%= link_to view_order_path do %>
      <span class="glyphicon glyphicon-shopping-cart"></span> View Cart
      <% end %>
    </li>
    <li>
    <% if user_signed_in? %>
    <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
    <% else %>
    <% link_to 'Sign In', new_user_session_path %>
    <% end %>
  </li>
</ul>
  </div>
</nav>
